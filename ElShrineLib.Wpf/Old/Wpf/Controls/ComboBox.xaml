<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:extension="clr-namespace:ElShrine.Old.Wpf.Controls.Extensions"
                    xmlns:wpf="clr-namespace:ElShrine.Old.Wpf"
                    xmlns:converter="clr-namespace:ElShrine.Old.Wpf.Converters">
    <Style x:Key="ElShrineComboBox_ToggleButton_BaseStyle" TargetType="{x:Type ToggleButton}">
        <Style.Resources>
            <converter:ToLightColorBrush x:Key="ToLightColorBrush"/>
            <ObjectDataProvider x:Key="OptionProvider" MethodName="GetInstance" ObjectType="{x:Type wpf:Option}"/>
        </Style.Resources>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <ControlTemplate.Resources>
                        <converter:ToOppsiteColorBrush x:Key="ToOppsiteColorBrush"/>
                        <converter:ToTargetColorBrush x:Key="ToTargetColorBrush"/>
                        <converter:ThicknessToDouble x:Key="ThicknessToDouble"/>
                    </ControlTemplate.Resources>
                    <Grid x:Name="rootGrid" Margin="0">
                        <Border x:Name="templateRoot" 
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding BorderThickness,Converter={StaticResource ThicknessToDouble},ConverterParameter=Max}" SnapsToDevicePixels="true">
                            <Border.Background>
                                <MultiBinding Converter="{StaticResource ToTargetColorBrush}" ConverterParameter="0.3">
                                    <Binding Path="Background" RelativeSource="{RelativeSource TemplatedParent}"/>
                                    <Binding Path="BorderBrush" RelativeSource="{RelativeSource Self}"/>
                                    <Binding Path="(extension:RateExtension.BackColorRate)" ElementName="templateRoot"/>
                                </MultiBinding>
                            </Border.Background>
                            <Border.BorderBrush>
                                <MultiBinding Converter="{StaticResource ToOppsiteColorBrush}" ConverterParameter="Brush_-0.4">
                                    <Binding Path="Foreground" RelativeSource="{RelativeSource TemplatedParent}"/>
                                    <Binding Path="(extension:RateExtension.ForeColorRate)" ElementName="templateRoot"/>
                                </MultiBinding>
                            </Border.BorderBrush>
                            <Border x:Name="splitBorder" 
                                BorderBrush="Transparent" BorderThickness="0" HorizontalAlignment="Right" Margin="0" SnapsToDevicePixels="true" Width="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}">
                                <Path x:Name="arrow" 
                                  Data="F1 M 0,0 L 2.667,2.66665 L 5.3334,0 L 5.3334,-1.78168 L 2.6667,0.88501 L0,-1.78168 L0,0 Z"
                                  HorizontalAlignment="Center" Margin="0" 
                                  VerticalAlignment="Center">
                                    <Path.Fill>
                                        <MultiBinding Converter="{StaticResource ToOppsiteColorBrush}" ConverterParameter="Brush_-0.4">
                                            <Binding Path="Foreground" RelativeSource="{RelativeSource TemplatedParent}"/>
                                            <Binding Path="(extension:RateExtension.ForeColorRate)" ElementName="templateRoot"/>
                                        </MultiBinding>
                                    </Path.Fill>
                                </Path>
                            </Border>
                        </Border>
                        <ContentControl>
                            <ContentControl.Foreground>
                                <MultiBinding Converter="{StaticResource ToOppsiteColorBrush}" ConverterParameter="Brush_-0.4">
                                    <Binding Path="Foreground" RelativeSource="{RelativeSource TemplatedParent}"/>
                                    <Binding Path="(extension:RateExtension.ForeColorRate)" ElementName="templateRoot"/>
                                </MultiBinding>
                            </ContentControl.Foreground>
                            <ContentPresenter x:Name="contentPresenter" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" IsHitTestVisible="false" Margin="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </ContentControl>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="Opacity" 
                                                To="0.6" 
                                                Duration="0:0:0.08">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="Opacity" 
                                                To="1" 
                                                BeginTime="0:0:0.05"
                                                Duration="0:0:0.16">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="Opacity" 
                                                To="1" 
                                                Duration="0:0:0.16">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="Opacity" 
                                                To="0.57" 
                                                BeginTime="0:0:0.05"
                                                Duration="0:0:0.16">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <EventTrigger RoutedEvent="Mouse.MouseEnter">
                            <EventTrigger.Actions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="(extension:RateExtension.ForeColorRate)" 
                                                To="1" 
                                                Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="(extension:RateExtension.BackColorRate)" 
                                                To="1" 
                                                Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger.Actions>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="Mouse.MouseLeave">
                            <EventTrigger.Actions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"  
                                                Storyboard.TargetProperty="(extension:RateExtension.ForeColorRate)" 
                                                To="0"  
                                                Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="(extension:RateExtension.BackColorRate)" 
                                                To="0" 
                                                Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger.Actions>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="OverridesDefaultStyle" Value="true"/>
        <Setter Property="IsTabStop" Value="false"/>
        <Setter Property="Focusable" Value="false"/>
        <Setter Property="ClickMode" Value="Press"/>
        <Setter Property="Background" Value="{Binding Option_BackColor, Source={StaticResource OptionProvider},Converter={StaticResource ToLightColorBrush},ConverterParameter=0.1}"/>
        <Setter Property="Foreground" Value="{Binding Option_ForeColor, Source={StaticResource OptionProvider}}"/>
    </Style>
    <Style x:Key="ElShrineComboBoxItem_BaseStyle" TargetType="{x:Type ComboBoxItem}">
        <Setter Property="FontSize" Value="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
        <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <ControlTemplate.Resources>
                        <converter:ToTargetColorBrush x:Key="ToTargetColorBrush"/>
                        <converter:ThicknessToDouble x:Key="ThicknessToDouble"/>
                        <converter:ThicknessFactor x:Key="ThicknessFactor"/>
                        <converter:DoubleFactor x:Key="DoubleFactor"/>
                    </ControlTemplate.Resources>
                    <Grid x:Name="templateRoot">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.Background>
                            <MultiBinding Converter="{StaticResource ToTargetColorBrush}" ConverterParameter="0.6">
                                <Binding Path="Background" RelativeSource="{RelativeSource Mode=TemplatedParent}"/>
                                <Binding Path="Foreground" RelativeSource="{RelativeSource Mode=TemplatedParent}"/>
                                <Binding Path="(extension:RateExtension.BackColorRate)" ElementName="templateRoot"/>
                            </MultiBinding>
                        </Grid.Background>
                        <Border x:Name="Sign_Bd"
                                Opacity="0" Width="{TemplateBinding BorderThickness,Converter={StaticResource ThicknessToDouble},ConverterParameter=Max_0.7}" BorderThickness="{TemplateBinding BorderThickness,Converter={StaticResource ThicknessFactor},ConverterParameter=0.35}" CornerRadius="{Binding Width,RelativeSource={RelativeSource Self},Converter={StaticResource DoubleFactor},ConverterParameter=0.5}" Margin="{TemplateBinding BorderThickness,Converter={StaticResource ThicknessFactor},ConverterParameter=0.8_T-0.75_B-0.75}" VerticalAlignment="Stretch">
                            <Border.BorderBrush>
                                <Binding Path="Foreground" RelativeSource="{RelativeSource Mode=TemplatedParent}"/>
                            </Border.BorderBrush>
                        </Border>
                        <Border x:Name="Bd" Grid.Column="1" Background="{TemplateBinding Background}" extension:RateExtension.BackColorRate="1" Margin="{TemplateBinding BorderThickness,Converter={StaticResource ThicknessFactor},ConverterParameter=1_L0_T0.5_R0.7_B0.5}" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <TextBlock Grid.Column="2" Text="{TemplateBinding IsSelected}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="Opacity" 
                                                To="1" 
                                                Duration="0:0:0.16">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="Opacity" 
                                                To="0.57" 
                                                BeginTime="0:0:0.05"
                                                Duration="0:0:0.16">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsSelected" Value="False"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard HandoffBehavior="Compose">
                                    <Storyboard>
                                        <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="(extension:RateExtension.ForeColorRate)" 
                                                To="1" 
                                                Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="(extension:RateExtension.BackColorRate)" 
                                                To="1" 
                                                Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation 
                                                Storyboard.TargetName="Sign_Bd"                    
                                                Storyboard.TargetProperty="Opacity" 
                                                To="1" 
                                                BeginTime="0:0:0.05"
                                                Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>
                            <MultiTrigger.ExitActions>
                                <BeginStoryboard HandoffBehavior="Compose">
                                    <Storyboard>
                                        <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"  
                                                Storyboard.TargetProperty="(extension:RateExtension.ForeColorRate)" 
                                                To="0"  
                                                Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="(extension:RateExtension.BackColorRate)" 
                                                To="0" 
                                                Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation 
                                                Storyboard.TargetName="Sign_Bd"                    
                                                Storyboard.TargetProperty="Opacity" 
                                                To="0" 
                                                BeginTime="0:0:0.2"
                                                Duration="0:0:0.5">
                                            <DoubleAnimation.EasingFunction>
                                                <SineEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.ExitActions>
                        </MultiTrigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" TargetName="templateRoot">
                                <Setter.Value>
                                    <MultiBinding Converter="{StaticResource ToTargetColorBrush}" ConverterParameter="0.6">
                                        <Binding Path="Background" RelativeSource="{RelativeSource Mode=TemplatedParent}"/>
                                        <Binding Path="Foreground" RelativeSource="{RelativeSource Mode=TemplatedParent}"/>
                                        <Binding Path="(extension:RateExtension.BackColorRate)" ElementName="Bd"/>
                                    </MultiBinding>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="False">
                            <Setter Property="Background" TargetName="templateRoot">
                                <Setter.Value>
                                    <MultiBinding Converter="{StaticResource ToTargetColorBrush}" ConverterParameter="0.6">
                                        <Binding Path="Background" RelativeSource="{RelativeSource Mode=TemplatedParent}"/>
                                        <Binding Path="Foreground" RelativeSource="{RelativeSource Mode=TemplatedParent}"/>
                                        <Binding Path="(extension:RateExtension.BackColorRate)" ElementName="templateRoot"/>
                                    </MultiBinding>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="ComboBoxEditableTextBox" TargetType="{x:Type TextBox}">
        <Setter Property="OverridesDefaultStyle" Value="true"/>
        <Setter Property="AllowDrop" Value="true"/>
        <Setter Property="MinWidth" Value="0"/>
        <Setter Property="MinHeight" Value="0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst"/>
        <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <ScrollViewer x:Name="PART_ContentHost" Background="Transparent" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <ControlTemplate x:Key="ElShrineComboBox_BaseTemplate" TargetType="{x:Type ComboBox}">
        <ControlTemplate.Resources>
            <converter:ToOppsiteColorBrush x:Key="ToOppsiteColorBrush"/>
            <converter:ToLightColorBrush x:Key="ToLightColorBrush"/>
            <converter:ThicknessToDouble x:Key="ThicknessToDouble"/>
            <converter:ThicknessFactor x:Key="ThicknessFactor"/>
        </ControlTemplate.Resources>
        <Grid x:Name="templateRoot" SnapsToDevicePixels="true" Focusable="False" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition MinWidth="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" Width="0"/>
            </Grid.ColumnDefinitions>
            <Popup x:Name="PART_Popup" Focusable="False" 
                       AllowsTransparency="true" Grid.ColumnSpan="2" IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Margin="0,0,0,0" Placement="Bottom" PlacementTarget="{Binding ElementName=templateRoot}" VerticalOffset="{TemplateBinding BorderThickness,Converter={StaticResource ThicknessToDouble},ConverterParameter=Min_-0.28}" PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}">
                <Border x:Name="dropDownBorder" Focusable="False" 
                            BorderBrush="{TemplateBinding Foreground}" CornerRadius="{TemplateBinding BorderThickness,Converter={StaticResource ThicknessToDouble},ConverterParameter=Max}" BorderThickness="{TemplateBinding BorderThickness,Converter={StaticResource ThicknessFactor},ConverterParameter=0.85_T0}" MinWidth="{Binding ActualWidth, ElementName=templateRoot}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                    <Border.Background>
                        <MultiBinding Converter="{StaticResource ToLightColorBrush}" ConverterParameter="0.9">
                            <Binding Path="Background" RelativeSource="{RelativeSource TemplatedParent}"/>
                            <Binding Path="(extension:RateExtension.BackColorRate)" ElementName="templateRoot"/>
                        </MultiBinding>
                    </Border.Background>
                    <ScrollViewer x:Name="DropDownScrollViewer" Focusable="False" IsTabStop="False">
                        <Grid x:Name="grid" RenderOptions.ClearTypeHint="Enabled">
                            <Canvas x:Name="canvas" Focusable="False"  HorizontalAlignment="Left" Height="0" VerticalAlignment="Top" Width="0">
                                <Rectangle x:Name="opaqueRect" Fill="{Binding Background, ElementName=dropDownBorder}" Height="{Binding ActualHeight, ElementName=dropDownBorder}" Width="{Binding ActualWidth, ElementName=dropDownBorder}"/>
                            </Canvas>
                            <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Grid>
                    </ScrollViewer>
                </Border>
            </Popup>
            <ToggleButton x:Name="toggleButton" Focusable="False" 
                              Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding Foreground}" BorderThickness="{TemplateBinding BorderThickness}" Grid.ColumnSpan="2" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Style="{StaticResource ElShrineComboBox_ToggleButton_BaseStyle}">
                <ToggleButton.Content>
                    <Control x:Name="toggleButtonInner" 
                             Foreground="{Binding Foreground,RelativeSource={RelativeSource AncestorType={x:Type ContentControl}}}"/>
                </ToggleButton.Content>
            </ToggleButton>
            <ContentControl Background="Transparent" 
                                Foreground="{Binding Foreground,ElementName=toggleButtonInner}" Focusable="False" >
                <ContentPresenter x:Name="contentPresenter" Focusable="False"  ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" Content="{TemplateBinding SelectionBoxItem}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" IsHitTestVisible="false" Margin="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
            </ContentControl>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="HasItems" Value="false">
                <Setter Property="Height" TargetName="dropDownBorder" Value="30"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsGrouping" Value="true"/>
                    <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false"/>
                </MultiTrigger.Conditions>
                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
            </MultiTrigger>
            <Trigger Property="ScrollViewer.CanContentScroll" SourceName="DropDownScrollViewer" Value="false">
                <Setter Property="Canvas.Top" TargetName="opaqueRect" Value="{Binding VerticalOffset, ElementName=DropDownScrollViewer}"/>
                <Setter Property="Canvas.Left" TargetName="opaqueRect" Value="{Binding HorizontalOffset, ElementName=DropDownScrollViewer}"/>
            </Trigger>

            <Trigger Property="IsEnabled" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="Opacity" 
                                                To="1" 
                                                Duration="0:0:0.16">
                                <DoubleAnimation.EasingFunction>
                                    <SineEase/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="Opacity" 
                                                To="0.57" 
                                                BeginTime="0:0:0.05"
                                                Duration="0:0:0.16">
                                <DoubleAnimation.EasingFunction>
                                    <SineEase/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
            <EventTrigger RoutedEvent="Mouse.MouseEnter">
                <EventTrigger.Actions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="(extension:RateExtension.ForeColorRate)" 
                                                To="1" 
                                                Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <SineEase/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="(extension:RateExtension.BackColorRate)" 
                                                To="1" 
                                                Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <SineEase/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger.Actions>
            </EventTrigger>
            <EventTrigger RoutedEvent="Mouse.MouseLeave">
                <EventTrigger.Actions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"  
                                                Storyboard.TargetProperty="(extension:RateExtension.ForeColorRate)" 
                                                To="0"  
                                                Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <SineEase/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="(extension:RateExtension.BackColorRate)" 
                                                To="0" 
                                                Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <SineEase/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger.Actions>
            </EventTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <ControlTemplate x:Key="ElShrineComboBox_EditableTemplate" TargetType="{x:Type ComboBox}">
        <ControlTemplate.Resources>
            <converter:ToOppsiteColorBrush x:Key="ToOppsiteColorBrush"/>
            <converter:ToLightColorBrush x:Key="ToLightColorBrush"/>
            <converter:ThicknessToDouble x:Key="ThicknessToDouble"/>
            <converter:ThicknessFactor x:Key="ThicknessFactor"/>
        </ControlTemplate.Resources>
        <Grid x:Name="templateRoot" SnapsToDevicePixels="true" Focusable="False">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition MinWidth="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" Width="0"/>
            </Grid.ColumnDefinitions>
            <Popup x:Name="PART_Popup" Focusable="False" 
                       AllowsTransparency="true" Grid.ColumnSpan="2" IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Margin="0,0,0,0" Placement="Bottom" PlacementTarget="{Binding ElementName=templateRoot}" VerticalOffset="{TemplateBinding BorderThickness,Converter={StaticResource ThicknessToDouble},ConverterParameter=Min_-0.28}" PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}">
                <Border x:Name="dropDownBorder" BorderBrush="{TemplateBinding Foreground}" CornerRadius="{TemplateBinding BorderThickness,Converter={StaticResource ThicknessToDouble},ConverterParameter=Max}" BorderThickness="{TemplateBinding BorderThickness,Converter={StaticResource ThicknessFactor},ConverterParameter=0.85_T0}" MinWidth="{Binding ActualWidth, ElementName=templateRoot}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                    <Border.Background>
                        <MultiBinding Converter="{StaticResource ToLightColorBrush}" ConverterParameter="0.9">
                            <Binding Path="Background" RelativeSource="{RelativeSource TemplatedParent}"/>
                            <Binding Path="(extension:RateExtension.BackColorRate)" ElementName="templateRoot"/>
                        </MultiBinding>
                    </Border.Background>
                    <ScrollViewer x:Name="DropDownScrollViewer" IsTabStop="False">
                        <Grid x:Name="grid" RenderOptions.ClearTypeHint="Enabled">
                            <Canvas x:Name="canvas" HorizontalAlignment="Left" Height="0" VerticalAlignment="Top" Width="0">
                                <Rectangle x:Name="opaqueRect" Fill="{Binding Background, ElementName=dropDownBorder}" Height="{Binding ActualHeight, ElementName=dropDownBorder}" Width="{Binding ActualWidth, ElementName=dropDownBorder}"/>
                            </Canvas>
                            <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Grid>
                    </ScrollViewer>
                </Border>
            </Popup>
            <ToggleButton x:Name="toggleButton" Focusable="False" IsTabStop="False"
                              Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding Foreground}" BorderThickness="{TemplateBinding BorderThickness}" Grid.ColumnSpan="2" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Style="{StaticResource ElShrineComboBox_ToggleButton_BaseStyle}">
                <ToggleButton.Content>
                    <Control x:Name="toggleButtonInner" 
                             Foreground="{Binding Foreground,RelativeSource={RelativeSource AncestorType={x:Type ContentControl}}}"/>
                </ToggleButton.Content>
            </ToggleButton>
            <Border x:Name="border"
                            Background="Transparent" Margin="{TemplateBinding BorderThickness}">
                <TextBox x:Name="PART_EditableTextBox" IsTabStop="True"
                                 Foreground="{Binding Foreground,ElementName=toggleButtonInner,Converter={StaticResource ToLightColorBrush},ConverterParameter=0.3}" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" IsReadOnly="{Binding IsReadOnly, RelativeSource={RelativeSource TemplatedParent}}" Margin="{TemplateBinding Padding}" Style="{StaticResource ComboBoxEditableTextBox}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
            </Border>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Opacity" TargetName="border" Value="0.56"/>
            </Trigger>
            <Trigger Property="HasItems" Value="false">
                <Setter Property="Height" TargetName="dropDownBorder" Value="95"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsGrouping" Value="true"/>
                    <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false"/>
                </MultiTrigger.Conditions>
                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
            </MultiTrigger>
            <Trigger Property="ScrollViewer.CanContentScroll" SourceName="DropDownScrollViewer" Value="false">
                <Setter Property="Canvas.Top" TargetName="opaqueRect" Value="{Binding VerticalOffset, ElementName=DropDownScrollViewer}"/>
                <Setter Property="Canvas.Left" TargetName="opaqueRect" Value="{Binding HorizontalOffset, ElementName=DropDownScrollViewer}"/>
            </Trigger>

            <Trigger Property="IsEnabled" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="Opacity" 
                                                To="1" 
                                                Duration="0:0:0.16">
                                <DoubleAnimation.EasingFunction>
                                    <SineEase/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="Opacity" 
                                                To="0.57" 
                                                BeginTime="0:0:0.05"
                                                Duration="0:0:0.16">
                                <DoubleAnimation.EasingFunction>
                                    <SineEase/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
            <EventTrigger RoutedEvent="Mouse.MouseEnter">
                <EventTrigger.Actions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="(extension:RateExtension.ForeColorRate)" 
                                                To="1" 
                                                Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <SineEase/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="(extension:RateExtension.BackColorRate)" 
                                                To="1" 
                                                Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <SineEase/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger.Actions>
            </EventTrigger>
            <EventTrigger RoutedEvent="Mouse.MouseLeave">
                <EventTrigger.Actions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"  
                                                Storyboard.TargetProperty="(extension:RateExtension.ForeColorRate)" 
                                                To="0"  
                                                Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <SineEase/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation 
                                                Storyboard.TargetName="templateRoot"                    
                                                Storyboard.TargetProperty="(extension:RateExtension.BackColorRate)" 
                                                To="0" 
                                                Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <SineEase/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger.Actions>
            </EventTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="ElShrineComboBox_BaseStyle" TargetType="{x:Type ComboBox}">
        <Style.Resources>
            <converter:ToLightColorBrush x:Key="ToLightColorBrush"/>
            <ObjectDataProvider x:Key="OptionProvider" MethodName="GetInstance" ObjectType="{x:Type wpf:Option}"/>
        </Style.Resources>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource ElShrineComboBoxItem_BaseStyle}"/>
        <Setter Property="IsEditable" Value="False"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Background" Value="{Binding Option_BackColor, Source={StaticResource OptionProvider}}"/>
        <Setter Property="Foreground" Value="{Binding Option_ForeColor, Source={StaticResource OptionProvider}}"/>
        <Setter Property="BorderThickness" Value="2.2"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="Padding" Value="9,3,5,3"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
        <Setter Property="ScrollViewer.PanningMode" Value="Both"/>
        <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
        <Setter Property="Template" Value="{StaticResource ElShrineComboBox_BaseTemplate}"/>
        <Style.Triggers>
            <Trigger Property="IsEditable" Value="true">
                <Setter Property="Template" Value="{StaticResource ElShrineComboBox_EditableTemplate}"/>
                <Setter Property="Background" Value="{Binding Option_BackColor, Source={StaticResource OptionProvider}}"/>
                <Setter Property="Foreground" Value="{Binding Option_ForeColor, Source={StaticResource OptionProvider}}"/>
                <Setter Property="BorderThickness" Value="2.2"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
                <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
                <Setter Property="Padding" Value="5,1,1,1"/>
                <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
                <Setter Property="ScrollViewer.PanningMode" Value="Both"/>
                <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>